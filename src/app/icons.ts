/**
 * Peso de trazo unificado para Lucide (iconografía coherente con UI semi-plana).
 * `soft` = títulos y bloques informativos; `medium` = barra y acciones; `strong` = logo y acentos.
 */
export const iconStroke = {
  soft: 1.75,
  medium: 2,
  strong: 2.25,
} as const
